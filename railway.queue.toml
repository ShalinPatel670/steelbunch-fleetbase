# Railway Configuration for Fleetbase Queue Worker
# Service: fleetbase-queue

[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile.queue"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
