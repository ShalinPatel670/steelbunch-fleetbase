# Railway Configuration for Fleetbase Scheduler
# Service: fleetbase-scheduler

[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile.scheduler"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
